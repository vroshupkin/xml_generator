{
	"watch": [
		"src"
	],
	"ext": "ts,json",
	"ignore": [
		"src/**/*.spec.ts"
	],
	"exec": "ts-node-esm --files ./src/main.ts",
    
    "env": {
        "NODE_OPTIONS":  "--experimental-specifier-resolution=node"
    }
}

